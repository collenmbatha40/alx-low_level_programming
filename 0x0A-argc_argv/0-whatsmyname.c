#include <stdio.h>
/**
 * main - prints its name, followed by a new line
 * @argc: counter for an argument
 * @argv: argument
 * Return: 0
 */
int main(char *argv[], int argc)
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
