#include "main.h"
#include <stdio.h>

/**
 *
 * add - adds two integers and returns the result
 *
 * Return:0
 *
*/
int add(int a, int b)
{
	int result = a + b;
	
	_putchar(result);
	return(result);
}
